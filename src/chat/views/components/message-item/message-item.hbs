<div class="message-item message-item--{{#if this.isOutgoing}}outgoing{{else}}incoming{{/if}}">
    {{#if this.message}}
        <div>
            {{{safeRender this.message}}}
        </div>
    {{/if}}

    {{#if this.images}}
        <div class="message-item__attached-images">
            {{#each this.images}}
                <img width="300" src="{{this.url}}" alt="{{this.title}}">
            {{/each}}
        </div>
    {{/if}}

    <div class="message-item__meta">
        {{#if this.isChecked}}
            <img src="/assets/icons/checked.svg" alt="Checked">
        {{/if}}

        <span class="message-item__time {{#if this.isOutgoing}}message-item__time--outgoing{{/if}}">
            {{formatTime this.date}}
        </span>
    </div>
</div>
