<li class="chat-item {{#if (eq this.id openedChat.id)}}chat-item--active{{/if}}" data-case="open-chat" data-chat-id="{{this.id}}">
    <div class="chat-item__wrapper">
        <div class="chat-item-avatar">
            <img
                class="chat-item-avatar__image"
                src={{#if this.avatar}}"{{{getResourceUrl this.avatar}}}"{{else}}"/assets/icons/default-avatar.svg"{{/if}}
                alt="{{this.title}}"
            >
        </div>
        <div class="chat-item__content">
            <div class="chat-item__row">
                <span class="chat-item__name">{{this.title}}</span>
                <span class="chat-item__time">{{formatChatItemDate this.date}}</span>
            </div>
            <div class="chat-item__row">
                <p class="chat-item__last-message">
                    {{#if this.isOutgoing}}
                        <span class="chat-item__outgoing">Вы:</span>
                    {{/if}}

                    {{this.lastMessage}}
                </p>

                {{#if this.unreadCount}}
                    <div class="chat-item__badge">{{this.unreadCount}}</div>
                {{/if}}
            </div>
        </div>
    </div>
</li>
