<button
    class="button button--{{theme}}"
    type="{{type}}"
    {{#if to}}data-to="{{to}}"{{/if}}
    {{#if form}}form="{{form}}"{{/if}}
    {{#if disabled}}disabled{{/if}}
>
    {{title}}
</button>
