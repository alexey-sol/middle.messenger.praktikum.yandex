export type DropdownItem = {
    dataCase: string;
    iconUrl?: string;
    title: string;
};

export type HasAvatarFile = {
    avatarFile: File | null;
};

export type HasId = {
    id: number;
};
